/**
 * This application runs zoneless (see `provideZonelessChangeDetection()` in `app.config.ts`) and no longer
 * requires the `zone.js` polyfill. This file is kept as an extension point for any future polyfills.
 */
